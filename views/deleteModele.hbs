<div class="container">
    <div>
        {{#if msg}}
        <h4>{{msg}}</h4>
        {{/if}}
        <form action="{{form_action}}" method="POST">
            <div class="form">
                <label for="delete">Voulez-vous vraiment supprimer {{data.nom_modele}} ?</label>
                <input type="hidden" name="id" id="delete" value="{{data.id_modele}}">
                <input type="submit" name="delete" value="Supprimer">
            </div>
        </form>
    </div>
</div>