<div class="wrapper">
    <div>
        <div>
            {{#if msg}}
            <h4>{{msg}}</h4>
            {{/if}}
        </div>
        <span class="existing">Utilisateurs existant :</span>
        <select>
            {{#each data}}
            <option value="{{this._id}}">{{this.firstName}}</option>
            {{/each}}
        </select>
        <form action="{{this.form_action}}" method="POST" id="form">
            <div class="form">
            <span class="err"></span>
                <label for="name">Nom* :</label>
                <input class="needed" type="text" name="name" id="name" value="{{data.name}}">
                <label for="firstName">Prénom* :</label>
                <input class="needed" type="text" name="firstName" id="firstName" value="{{data.firstName}}">
                <label for="login">Pseudo* :</label>
                <input class="needed" type="text" name="login" id="login" value="{{data.login}}">
                <label for="mdp">Mot de passe* :</label>
                <input class="needed pass" type="password" name="mdp" id="mdp" value="{{data.mdp}}">
                <label for="password-confirm">Confirmation du mot de passe* :</label>
                <input class="needed confirm" type="password" name="password-confirm" id="password-confirm">
                <input type="submit" value="Ajoutez">
                <span class="champs">*champs requis</span>
            </div>
        </form>
    </div>
</div>
<script src="js/form.js"></script>